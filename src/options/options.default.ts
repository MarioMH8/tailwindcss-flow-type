import type { TailwindFlowTypeOptions } from './options.type';

const DEFAULT_OPTIONS: TailwindFlowTypeOptions = {
	fontSizeMax: 1.25,
	fontSizeMin: 1.125,
	override: false,
	prefix: 'flow',
	ratioMax: 1.2,
	ratioMin: 1.125,
	screenMax: 96,
	screenMin: 20,
	unit: 'rem',
};

export default DEFAULT_OPTIONS;
